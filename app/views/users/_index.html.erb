<div class="container">
  <div class="row">
    <div class="col-md-3">
<h2>User info</h2>
  <div>
    <div class="border-bottom m-4 " ><%= image_tag @user.get_image %></div>
    <div class="border-bottom m-4 " >name<%= @user.name %></div>
    <div class="m-4" >introduction<%= @user.introduction %></div>
  </div>
<div class="d-flex justify-content-center border rounded p-2">
<%= link_to "", edit_user_path(@user.id), class:"fa-solid fa-user-gear" %>
</div calss ="p-3">
<h2>New Book</h2>
<%= form_with model: @book, url: '/books',method: :post do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>

<h4>Option</h4>
<%= f.text_area :body %>

<%= f.submit "Create Book"  %>
<% end %>
<div>
</div>
</div>


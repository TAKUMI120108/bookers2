<div class="container"> 
<div class="row"> 
<div class="col-md-3">

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'index', user:@user, book:@book %>
</div>
<div class="col-md-8 offset-md-1">
  
<h2>Books</h2>

<table class="table">
  <thead>
    <tr>
      <th class="col">Image</th>
      <th class="col">name</th>
      <th colspan="2"></th>
    </tr>
  </thead>

<tbody>
<% @users.each do |user| %>
<tr>
  <td class="col" >
  <%= image_tag user.get_profile_image %></td>
  <td class="col"><%= user.name  %></td>
  <td class="col"><%= link_to "Show", user_path(user.id) %></td>
  </tr>
<% end %>
</tbody>
</table>
</div>
</div>
</div>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'index', users:@user %>

<div class="offset-md-1"></div>
<div class="col-md-8">

<h2>Books</h2>

<table class="table">
  <thead>
    <tr class="d-flex justify-content-around">
      <th class="col">Image</th>
      <th class="col">name</th>
      <th colspan="2"></th>
    </tr>
  </thead>

<tbody>
<% @users.each do |user| %>
<tr class="d-flex justify-content-around　align-middle">
       <td class="col" >
           <%= image_tag @user.get_image %></td>
<td class="col"><%= user.name  %></td>
<td class="col"><%= link_to "Show", user_path(current_user) %></td>
<% end %>
</tbody>

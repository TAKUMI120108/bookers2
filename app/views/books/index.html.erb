<%= render 'users/index', users:@user %>

<div class="offset-md-1"></div>
<div class="col-md-8">

<h2>Book detail</h2>

<table class="table">
  <thead>
    <tr class="d-flex justify-content-around">
      <th colspan="2">Title</th>
      <th class="col">Opinion</th>
    </tr>
  </thead>
  
<tbody>
<% @books.each do |book| %>
<tr class="d-flex justify-content-around　align-middle">
       <td class="col border-bottom m-4 " >
           <%= image_tag @user.get_image %></td>
<td class="col"><%= book.title %></td>
<td class="col"><%= book.body %></td>
<% end %>
</tbody>
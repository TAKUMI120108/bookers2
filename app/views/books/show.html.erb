<div class="container"> 
<div class="row"> 
<div class="col-md-3">
      
<% if @book.errors.any? %>
 <%= @book.errors.count %>errors prohibited this obj from being saved
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>      

<%= render 'users/index', user:@user, book:@book_create %>

</div>
<div class="col-md-8 offset-md-1">

<h1>Book detail</h1>

<div class="d-flex justify-content-around border-top">

      <div class="m-1 " ><%=link_to user_path(@user) do %>
          <%= image_tag @user.get_profile_image %>
          <% end %></div>
      <div class="m-1 " ><%= link_to @user.name, user_path(@user.id)%></div>
      <div class="m-1"><%= link_to @book.title, book_path(@book.id) %></div>
      <div class="m-1"><%= @book.body %></div>
      <% if @book.user == current_user %>
      <div class="m-1"><%= link_to 'Edit', edit_book_path(@book.id) %></div>
      <div class="m-1"></div><%= link_to 'Destroy', book_path(@book.id), method: :delete, "data-confirm" => "Are you sure?" %>
       <% end %>
       </div>
       </div>
       </div>
       </div>
